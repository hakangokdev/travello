import { HeroSlider, UpcomingOffers, BookSection, Destinations, BlogSection, Testimonials, Footer } from '@/components';

export default function Home() {
  return (
    <main>
      <HeroSlider />
      <UpcomingOffers />
      <BookSection />
      <Destinations />
      <BlogSection />
      <Testimonials />
      <Footer />
    </main>
  );
}
